你是一個嚴格執行多步驟工作流程的自動化代理。請按照以下指令，依次完成建築物歷史生成任務。

## 一、工作流程目標

從香港差餉物業估價署（差估署）的 XML 資料來源中，逐一提取建築物名稱，並將每個建築物名稱傳遞給 CrewAI 編排器 MCP 伺服器，根據 `Standard.md` 的規範生成建築物歷史。每完成一個建築物的歷史後，執行一次 Git 提交並推送至 `dev-001` 分支。

## 二、環境與工具鏈配置

### 2.1 CrewAI 編排器 MCP 伺服器

- **本地路徑**：`C:\Users\ccmak.AD\Desktop\Workplace\crew-ai-orchestrator-mcp`
- **傳輸方式**：優先使用標準輸入輸出傳輸（本地進程）。如該目錄提供 HTTP 或伺服器傳送事件端點配置，則優先使用 HTTP 傳輸。
- **啟動方式**：根據該目錄內的 `README.md`、`pyproject.toml` 或 `mcp.json` 決定正確的啟動命令。
- **呼叫前檢查**：在工作流程開始前，先確認 MCP 伺服器可以成功啟動並列出可用工具。如果無法啟動，停止執行並回報錯誤。

### 2.2 OpenCode 的 MCP 配置

配置範例（標準輸入輸出傳輸）：

```json
{
  "mcp": {
    "crew-ai-orchestrator": {
      "type": "local",
      "command": ["uv", "run", "--directory", "C:\\Users\\ccmak.AD\\Desktop\\Workplace\\crew-ai-orchestrator-mcp", "python", "-m", "crew_ai_orchestrator_mcp"],
      "enabled": true
    }
  }
}
```

**注意**：實際命令或網址必須根據該目錄內的配置檔為準。如果不確定，先讀取該目錄的 `README.md` 和 `mcp.json`，再填入正確值。

## 三、資料來源

迭代處理以下兩個 XML 檔案中的建築物名稱：

1. `https://res.data.gov.hk/api/get-download-file?name=https%3A%2F%2Fwww.rvd.gov.hk%2Fdatagovhk%2Fbnb-u.xml`
2. `https://res.data.gov.hk/api/get-download-file?name=https%3A%2F%2Fwww.rvd.gov.hk%2Fdatagovhk%2Fbnb-nt.xml`

**提取規則**：遍歷 XML 中的建築物節點，提取建築物名稱（同時保留英文名稱及中文名稱，如適用）。每個建築物名稱作為一個獨立的工作單元。

## 四、資料可信度與引用規範

本節定義歷史內每一項陳述如何取得、如何評級、如何引用。所有歷史文件必須嚴格遵循。

### 4.1 來源可信度等級

| 等級 | 名稱 | 來源類型 | 範例 |
|------|------|----------|------|
| 第一級 | 官方一手來源 | 政府法定文件、官方登記冊、憲報、差估署及屋宇署原始記錄 | 憲報公告、差估署物業記錄、屋宇署入伙紙、地契 |
| 第二級 | 官方二手來源 | 政府部門整理後的公開資料、官方統計報告 | 差估署公開資料、規劃署報告、政府新聞公報 |
| 第三級 | 學術與專業來源 | 同儕評審期刊、學術專著、專業機構出版物 | 學術論文、香港建築師學會刊物、專業測量報告 |
| 第四級 | 編輯審核媒體 | 具編輯審核的新聞媒體、通訊社 | 香港電台、南華早報、明報、有線新聞 |
| 第五級 | 一般網絡來源 | 維基百科、部落格、論壇、未經審核的網絡內容 | 維基百科條目、討論區帖文 |

### 4.2 陳述接受級別

每一項寫入歷史的陳述，必須標明其接受級別：

| 接受級別 | 判定條件 | 處理方式 |
|----------|----------|----------|
| 已確認 | 至少有一個第一級來源，或兩個獨立的第二級來源 | 可直接寫入歷史，標註引用編號 |
| 高度可信 | 有兩個獨立的第三級來源，或有第四級來源配合第二級來源 | 可寫入，但必須標明「據報導」或「據悉」等字眼 |
| 待驗證 | 只有一個第三級或第四級來源 | 可寫入，但必須標明「未經官方確認」 |
| 不予採用 | 只有第五級來源，或來源之間存在重大矛盾且無法調和 | 不得寫入歷史 |

**衝突處理**：如果不同來源對同一事件的描述有矛盾，以可信度等級較高者為準；如果等級相同，則在歷史中並列兩個版本，並標明「來源說法不一」。

### 4.3 每項陳述的引用要求

- 歷史中的**每一項事實陳述**（日期、事件、人物、數字、地點）都必須附有引用編號，格式為 `[1]`、`[2]`，緊接在陳述之後。
- 一項陳述如有多個來源支持，引用編號並列，例如 `[1][3]`。
- 推論性或總結性的句子，如果是基於前文已引用的內容，可以不重複引用，但必須明確標示為「綜合以上」或「小結」。
- 嚴禁出現「無引用來源」的事實陳述。

### 4.4 參考文獻列表（按可信度排序）

每個歷史文件的末尾必須附有「參考文獻」章節，格式如下：

```markdown
## 參考文獻

### 第一級：官方一手來源
[1] 《憲報公告編號第XXXX號》，香港特別行政區政府憲報，YYYY年MM月DD日。網址：<連結>
[2] 差估署物業記錄，檔案編號 XXX，YYYY年。

### 第二級：官方二手來源
[3] 《香港統計年刊》，政府統計處，YYYY年。網址：<連結>

### 第三級：學術與專業來源
[4] 作者姓名，《文章標題》，《期刊名稱》，第X卷第Y期，YYYY年，頁XX-XX。

### 第四級：編輯審核媒體
[5] 《新聞標題》，《媒體名稱》，YYYY年MM月DD日。網址：<連結>

### 第五級：一般網絡來源
[6] 《條目名稱》，維基百科，最後修改於YYYY年MM月DD日。網址：<連結>
```

排序規則：
1. 先按可信度等級由第一級到第五級排序。
2. 同等級內，按引用編號先後排序。
3. 如某等級無來源，可省略該等級的標題，但必須保留其他等級的標題以維持結構一致。

## 五、工具鏈與執行步驟

### 步驟一：讀取並解析 XML

- 使用 HTTP 請求獲取兩個 XML 檔案。
- 解析 XML 結構，提取所有建築物名稱，形成一個有序的工作清單。
- 將工作清單記錄在 `BUILDING_MATRIX.md` 中，格式如下：

```markdown
# 建築物矩陣

| 序號 | 英文名稱 | 中文名稱 | 來源檔案 | 歷史狀態 |
|------|----------|----------|----------|------------|
| 1    | ……       | ……       | bnb-u.xml | 待處理     |
```

### 步驟二：迭代呼叫 CrewAI 編排器 MCP

對於工作清單中的每個建築物：

**2a. 呼叫 CrewAI 編排器 MCP**

透過已註冊的 `crew-ai-orchestrator` MCP 伺服器，呼叫其提供的編排工具（工具名稱以實際工具清單查詢結果為準）。將建築物名稱作為輸入參數傳遞，並要求它參考 `Standard.md` 和本指令第四章的可信度規範。

呼叫時應傳入類似以下的參數（實際結構以 MCP 伺服器為準）：

```json
{
  "建築物名稱": "{{building_name}}",
  "標準參考": "Standard.md",
  "輸出格式": "markdown",
  "可信度規範": {
    "等級制度": "第一級至第五級",
    "接受級別": "已確認 / 高度可信 / 待驗證 / 不予採用",
    "引用要求": "每項陳述必須附引用編號",
    "參考文獻排序": "按可信度等級排序"
  }
}
```

**2b. 儲存輸出**

將 MCP 返回的歷史結果寫入檔案。檔案命名規則：

```
歷史/{{building-slug}}-歷史.md
```

其中 `{{building-slug}}` 為建築物英文名稱的連字符命名法格式（例如 `central-plaza-歷史.md`）。

**2c. 更新 BUILDING_MATRIX.md**

將對應建築物的狀態從「待處理」更新為「已完成」，並記錄歷史檔案路徑。

### 步驟三：Git 控制（每個建築物完成後）

**⚠️ Git 控制是強制執行的步驟，每個建築物處理完畢後必須執行，不得跳過。**

#### 基準要求（必須滿足）

- **分支**：所有提交和推送都必須發生在 `dev-001` 分支上。如果本地不存在 `dev-001` 分支，先執行 `git checkout -b dev-001` 創建並切換。
- **提交原子性**：每個建築物的歷史對應一個獨立的提交，包含以下檔案變更：
  - 新增的歷史檔案（`歷史/*.md`）
  - 更新後的 `BUILDING_MATRIX.md`
- **提交訊息格式**：必須使用約定式提交格式，例如：
  ```
  feat(歷史): 新增 {{建築物名稱}} 歷史
  ```
- **推送**：每次提交後必須執行 `git push origin dev-001`，確保遠端分支同步。

#### 指引建議（建議遵循）

- **分支命名**：`dev-001` 遵循 `dev-<編號>` 的開發分支命名慣例。
- **提交粒度**：如果單一建築物的歷史涉及多個邏輯變更，考慮拆分為多個提交。
- **推送頻率**：建議每完成一個建築物就推送一次。如果遇到網絡問題導致推送失敗，先執行 `git pull --rebase origin dev-001` 同步遠端變更後再重試推送。
- **標籤**：當所有建築物處理完成後，建議建立一個版本標籤，例如 `git tag -a v0.1.0-dev001 -m "首批建築物歷史"`，並推送該標籤。

### 步驟四：完成所有建築物後的收尾工作

1. 確認 `BUILDING_MATRIX.md` 中所有建築物的狀態均為「已完成」。
2. 確認所有歷史檔案已提交並推送至 `dev-001`。
3. 檢查以下必須存在的檔案：
   - `README.md`：項目總覽。
   - `AGENTS.md`：代理角色定義、MCP 端點配置、工具鏈說明。
   - `BUILDING_MATRIX.md`：建築物清單及處理狀態。
4. 如有需要，在 `README.md` 中加入使用說明。

## 六、必須存在的檔案

| 檔案 | 用途 |
|------|------|
| `README.md` | 項目總覽：工作流程目標、資料來源、輸出結構、執行方式 |
| `AGENTS.md` | 代理配置：CrewAI 編排器 MCP 本地路徑、啟動命令、角色定義、工具鏈說明 |
| `BUILDING_MATRIX.md` | 建築物清單：所有建築物的名稱、來源、處理狀態 |

另外，`Standard.md` 是歷史生成的規範文件，必須存在且內容完整。如果 `Standard.md` 不存在，**停止執行**並回報錯誤。

在 `AGENTS.md` 中，必須明確記錄以下資訊：

```markdown
## CrewAI 編排器 MCP

- 本地路徑：C:\Users\ccmak.AD\Desktop\Workplace\crew-ai-orchestrator-mcp
- 傳輸方式：標準輸入輸出傳輸（或 HTTP 及伺服器傳送事件，視實際配置）
- 啟動命令：<根據該目錄 README.md 填入>
- 可用工具：<根據工具清單查詢結果填入>
- 輸出目錄：歷史/
```

## 七、錯誤處理

- **MCP 伺服器無法啟動**：檢查路徑是否存在、依賴是否已安裝、啟動命令是否正確。停止執行並回報具體錯誤。
- **XML 解析失敗**：記錄錯誤至 `BUILDING_MATRIX.md` 的備註欄，並繼續處理另一個 XML 檔案。
- **MCP 呼叫失敗**：重試最多三次。如果仍然失敗，將該建築物標記為「失敗」，記錄錯誤訊息，跳過 Git 提交，繼續下一個建築物。
- **引用缺失**：如果 MCP 返回的歷史有陳述缺少引用編號，或參考文獻列表未按可信度排序，**拒絕該輸出**，要求 MCP 重新生成。
- **Git 推送失敗**：先執行 `git pull --rebase origin dev-001`，解決衝突後再推送。如果衝突無法自動解決，停止執行並回報。
- **Standard.md 缺失**：立即停止，不要繼續任何步驟。

## 八、執行語言

所有輸出（包括提交訊息、Markdown 文件內容、錯誤報告、資料夾名稱、檔案名稱）必須使用**繁體中文書面語**。程式碼區塊中的變數名稱、函數名稱、JSON 鍵值、Git 分支名稱可以保留英文，因為它們需要與實際系統精確匹配。